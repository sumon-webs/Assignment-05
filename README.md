
1️⃣ What is the difference between var, let, and const?
Ans: let and const are es6 version. Let is reassignable,block scope and cannot redeclare. Const is block scope, cannot reassign and redeclare. Var is Functional scope can redeclare and reassign.

2️⃣ What is the spread operator (...)?
Ans: Spread operation is es6 version, it is used to expand iterable objects;

3️⃣ What is the difference between map(), filter(), and forEach()?
Ans: map() return every element with an array, filter() return an new array containing only the same element that fullfil with condition and forEach() run a function on each element on an array.

4️⃣ What is an arrow function?
Ans: Arrow function is es6 version. It is shorter syntex of a regular function.

5️⃣ What are template literals?
Ans: Template literals include in es6. It is powerful way to create string.